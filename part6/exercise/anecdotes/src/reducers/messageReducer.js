import { createSlice } from '@reduxjs/toolkit';

const initialState = 'Full Stack Open - Part 6!!';

const messageSlice = createSlice({
  name: 'messages',
  initialState,
  reducers: {
    
  }
});

export default messageSlice.reducer;